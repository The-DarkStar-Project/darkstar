[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast offline unit and contract tests
    smoke: smoke tests for app routes and static assets
    playwright: browser-based Playwright tests
